# 修改顺序

按这个顺序推进，前一步完成后再进入下一步。

## 1. 定版修改方案

- 先把 `修改方案.md` 收口到最终版。
- 补齐你刚确认的收尾规则。
- 确认公共协议、降级规则、欢迎语和交付规则不再变。

## 2. 对齐公共协议

- 统一 `agent_result` 状态外壳。
- 统一产物契约、`ChangeLog`、`SourceRegistry`。
- 统一执行图、并行规则和回溯规则。

## 3. 按依赖顺序改提示词

建议顺序：

1. `Intake Agent`
2. `Template Analyst`
3. `Content Analyst`
4. `Outline Architect`
5. `Section Writers`
6. `Integrator`
7. `Citation Verifier`
8. `全文 AI 味审查代理`
9. `Compliance Auditor`
10. `Delivery Agent`
11. `Dispatcher`

原则：

- 先上游，后下游。
- 先定义输入输出，再写具体行为。
- 先保留硬约束，再写可选分支。

## 4. 同步架构文件

- 更新 JSON Schema。
- 更新配置文件。
- 更新调度规则。
- 清理旧的 7-Agent 口径。

## 5. 验证回归

- 检查修改后是否和 `修改方案.md` 一致。
- 检查提示词是否能接住公共协议。
- 检查降级路径是否闭环。
- 检查欢迎语、联网检索和 AI 味审查是否仍然保留。

## 6. 收口

- 方案、提示词、Schema、配置一致后，再进入正式测试。
- 如有冲突，优先回到 `修改方案.md` 修正，不直接在提示词里绕开。
